# Реализация кнопок Telegram в Okama Finance Bot

## Обзор изменений

Добавлены интерактивные кнопки Telegram в команды `/start` и `/namespace` для улучшения пользовательского опыта и быстрого доступа к основным функциям. **Кнопки теперь работают как прямые ссылки на готовые команды**, а не как callback-функции.

## Настройка ссылок

### Переменная конфигурации
В файле `config.py` добавлена переменная `BOT_FULL_NAME` для настройки ссылок:

```python
# Полное имя бота для ссылок (например: @okama_finance_bot)
BOT_FULL_NAME = os.getenv('BOT_FULL_NAME', '@okama_finance_bot')
```

### Настройка в .env файле
В файле `config_files/config.env.example` добавлена строка:

```bash
# Полное имя бота для ссылок (например: @okama_finance_bot)
BOT_FULL_NAME=@your_bot_username
```

**Важно**: Замените `@your_bot_username` на реальное имя вашего бота (например, `@okama_finance_bot`).

## Команда /start

### Добавленные кнопки:
- **📊 Информация об активе** - ведет на `https://t.me/ваш_бот?start=info`
- **📚 Пространства имен** - ведет на `https://t.me/ваш_бот?start=namespace`

### Функциональность:
- При нажатии на кнопку "Информация об активе" показывается подробная справка по использованию команды `/info`
- При нажатии на кнопку "Пространства имен" показывается справка по использованию команды `/namespace`
- Все кнопки ведут на прямые ссылки, которые автоматически выполняют соответствующие команды

## Команда /namespace

### Добавленные кнопки:
- **🇺🇸 US акции** - ведет на `https://t.me/ваш_бот?start=namespace_US`
- **🇷🇺 MOEX** - ведет на `https://t.me/ваш_бот?start=namespace_MOEX`
- **📈 Индексы** - ведет на `https://t.me/ваш_бот?start=namespace_INDX`
- **💱 Валюты** - ведет на `https://t.me/ваш_бот?start=namespace_FX`
- **🪙 Товары** - ведет на `https://t.me/ваш_бот?start=namespace_COMM`
- **🏦 CBR** - ведет на `https://t.me/ваш_бот?start=namespace_CBR`
- **🇬🇧 LSE** - ведет на `https://t.me/ваш_бот?start=namespace_LSE`
- **🇩🇪 XETR** - ведет на `https://t.me/ваш_бот?start=namespace_XETR`

### Функциональность:
- При нажатии на любую кнопку показываются первые 10 символов из соответствующего пространства имен
- **Каждый символ в списке теперь является кликабельной ссылкой** в названии символа в таблице
- Ссылки ведут на команды вида `https://t.me/ваш_бот?start=info_СИМВОЛ`
- Добавлены кнопки "🔙 К списку пространств" для навигации

## Кликабельные символы в таблице

### Новое в команде /namespace:
- **Каждое название символа теперь является кликабельной ссылкой** в таблице вместо кнопок
- **Формат таблицы**: `• **СИМВОЛ** - Название актива | Страна | Валюта`
- **Ссылка**: `https://t.me/ваш_бот?start=info_СИМВОЛ`
- При клике на символ автоматически выполняется команда `/info СИМВОЛ`

### Примеры кликабельных символов в таблице:
- `• **AAPL** - Apple Inc. | United States | USD` → `/info AAPL.US`
- `• **SBER** - Сбербанк России | Russia | RUB` → `/info SBER.MOEX`
- `• **GC** - Gold | United States | USD` → `/info GC.COMM`

### Преимущества табличного формата:
- ✅ **Компактность**: Больше информации в одном сообщении
- ✅ **Читаемость**: Структурированный вид данных
- ✅ **Кликабельность**: Каждый символ является ссылкой
- ✅ **Информативность**: Показывает страну и валюту для каждого символа

## Технические изменения

### 1. Обновлен метод `_send_message_safe`
- Добавлена поддержка параметра `reply_markup` для отправки сообщений с кнопками
- Реализована логика для длинных сообщений с кнопками (первая часть отправляется с кнопками)

### 2. Добавлена обработка параметров start в команде `/start`
- `start=info` - показывает справку по команде `/info`
- `start=namespace` - показывает справку по команде `/namespace`
- `start=namespace_КОД` - показывает символы в конкретном пространстве имен
- `start=info_СИМВОЛ` - выполняет команду `/info` для указанного символа

### 3. Новые вспомогательные методы
- `show_info_help()` - показывает справку по команде `/info`
- `show_namespace_help()` - показывает справку по команде `/namespace`
- `show_namespace_symbols()` - показывает символы в пространстве имен с кликабельными ссылками в таблице

### 4. Замена кнопок на кликабельные ссылки в таблице
- Убраны интерактивные кнопки для символов
- Добавлены кликабельные ссылки в названиях символов в таблице
- Каждая ссылка ведет на прямую ссылку для выполнения команды `/info`
- Используется MarkdownV2 для форматирования ссылок

### 5. Настройка ссылок через конфигурацию
- Все ссылки теперь используют переменную `Config.BOT_FULL_NAME`
- Ссылки автоматически формируются в формате: `https://t.me/{имя_бота}?start=...`
- Легко изменить имя бота через переменную окружения `BOT_FULL_NAME`

## Преимущества новой реализации

1. **Прямые ссылки**: Кнопки работают как обычные ссылки, не требуют callback-обработки
2. **Кликабельные символы в таблице**: Каждый символ в таблице можно кликнуть для получения информации
3. **Улучшенная навигация**: Легко переходить между разными экранами
4. **Совместимость**: Работает с любыми версиями Telegram
5. **Простота**: Не требует сложной логики обработки callback-запросов
6. **Гибкость**: Легко изменить имя бота через конфигурацию
7. **Компактность**: Больше информации в одном сообщении
8. **Структурированность**: Четкий табличный формат с данными

## Настройка и развертывание

### 1. Настройка переменных окружения
Создайте файл `config.env` на основе `config_files/config.env.example`:

```bash
# Telegram Bot Token (get from @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# YandexGPT API Configuration (get from https://cloud.yandex.com/)
YANDEX_API_KEY=your_yandex_api_key_here
YANDEX_FOLDER_ID=your_yandex_folder_id_here

# Bot Configuration (optional)
BOT_USERNAME=your_bot_username
# Полное имя бота для ссылок (например: @okama_finance_bot)
BOT_FULL_NAME=@your_bot_username
ADMIN_USER_ID=your_telegram_user_id
```

### 2. Важные моменты настройки
- **BOT_FULL_NAME**: Должно начинаться с `@` (например: `@okama_finance_bot`)
- **BOT_USERNAME**: Без символа `@` (например: `okama_finance_bot`)
- Все ссылки автоматически формируются на основе `BOT_FULL_NAME`

## Примеры использования

### Команда /start:
```
/start
```
Показывает приветственное сообщение с двумя кнопками для быстрого доступа к основным функциям.

### Команда /namespace:
```
/namespace
```
Показывает список всех пространств имен с кнопками для быстрого доступа к популярным категориям.

### Навигация по ссылкам:
1. Нажать на кнопку "🇺🇸 US акции"
2. Увидеть первые 10 символов из пространства US в табличном формате
3. Кликнуть на любой символ в таблице (например, **AAPL**)
4. Автоматически выполнится команда `/info AAPL.US`

### Прямые ссылки:
- `https://t.me/ваш_бот?start=namespace_MOEX` - сразу показывает символы MOEX в таблице
- `https://t.me/ваш_бот?start=info_SBER` - сразу выполняет `/info SBER.MOEX`

## Заключение

Реализация кнопок Telegram с прямыми ссылками и кликабельными символами в таблице значительно улучшает пользовательский опыт, делая бота более интуитивным и удобным в использовании. Пользователи могут:

- Быстро получать доступ к основным функциям через кнопки
- Кликать на любые символы в таблице для получения подробной информации
- Использовать прямые ссылки для быстрого доступа к нужным функциям
- Легко навигировать между разными экранами
- Получать структурированную информацию в компактном табличном формате

Все изменения полностью интегрированы с существующим кодом и сохраняют обратную совместимость с текстовыми командами. Настройка ссылок через переменную окружения делает бота гибким и легко настраиваемым.
